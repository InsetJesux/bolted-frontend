import { InputValidPipe } from './input-valid.pipe';

describe('InputValidPipe', () => {
  it('create an instance', () => {
    const pipe = new InputValidPipe();
    expect(pipe).toBeTruthy();
  });
});
